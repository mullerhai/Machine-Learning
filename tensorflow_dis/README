4个tensorflow分布式例子：model_parallel.py、data_parallel.py 、mnist_replica.py、census_widendeep.py。每个例子运行步骤如下。

1、model_parallel.py，简单的模型并行化例子，2个parameter-server、1个worker，参数存在parameter-server，worker节点训练时从parameter-server取数据，运行步骤：  
    1)启动parameter-server以及worker节点，运行命令：sh model_parallel_server.sh
    2)构建计算图，放到worker节点计算，运行命令：python model_parallel.py
    3)计算结束后停止parameter-server以及worker节点，运行命令：sh stop.sh

2、data_parallel.py，简单的数据并行化同步训练例子，1个master节点，2个worker节点，参数存在master节点，worker节点训练时从master取参数，计算损失和梯度，汇总到master取平均,更新参数，运行步骤：  
    1)启动master以及worker节点，运行命令：sh data_parallel_server.sh
    2)构建计算图，放到worker节点计算，运行命令：python data_parallel.py
    3)计算结束后停止master以及worker节点，运行命令：sh stop.sh


3、mnist_replica.py,手写识别数据并行化训练例子，这个例子是tensorflow测试使用的例子(https://github.com/tensorflow/tensorflow/blob/master/tensorflow/tools/dist_test/python/mnist_replica.py)，运行步骤：
    1)到相应的机器上启动parameter-server以及worker节点，比如集群拓扑是：worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:2224，表示有2个worker节点（10.13.82.20:2222;10.13.89.97:2222），一个parameter-server节点(10.13.89.97:2224)，则需要到10.13.82.20机器运行命令：python grpc_tensorflow_server.py --cluster_spec 'worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:2224' --job_name worker --task_id 0，到10.13.89.97机器运行命令：python grpc_tensorflow_server.py --cluster_spec 'worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:2224' --job_name worker --task_id 1，到10.13.89.97运行命令：python grpc_tensorflow_server.py --cluster_spec 'worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:2224' --job_name ps --task_id 0
    2)构建计算图，放到worker节点上计算(注：第一个worker节点，只能起一个；其它的可起多个并行跑)，比如拓扑结构和1)中一样,则可以运行命令(参数含义可查看mnist_replica.py中注释)：
    python ./mnist_replica.py --cluster_spec 'worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:222' --worker_index 0 --num_workers 2 --num_parameter_servers 1 --worker_grpc_url grpc://10.13.89.97:2222
    python ./mnist_replica.py --cluster_spec 'worker|10.13.82.20:2222;10.13.89.97:2222,ps|10.13.89.97:222' --worker_index 1 --num_workers 2 --num_parameter_servers 1 --worker_grpc_url grpc://10.13.82.20:2222
    3)算结束后到相应的机器上停止parameter-server以及worker节点，运行命令：sh stop.sh

4、census_widendeep.py分布式深广模型，也是tensorflow测试使用的例子(https://github.com/tensorflow/tensorflow/blob/master/tensorflow/tools/dist_test/python/census_widendeep.py)，还没有跑过这个例子
